{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Chrome (EconNews)",
            "type": "chrome",
            "request": "launch",
            "file": "${workspaceFolder}/index.html"
        },
        {
            "name": "Launch Edge (EconNews)",
            "type": "msedge",
            "request": "launch",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}